{
  "build": {
    "builder": "NIXPACKS",
    "buildpacks": ["nodejs", "nodejs-npm@8"]
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "cd frontend && npm run build && npm run build --workspace=root"
  }
}