{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && pip install streamlink --break-system-packages || true && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
